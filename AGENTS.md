# 法律条文标注系统开发约束

## 业务基线

- 两份 2026-08-19 V1.5 文档是当前业务基线：
  - 《法律条文标注系统 Codex 项目上下文与开发边界》
  - 《法律条文标注系统 需求规格说明书与团队开发对接文档》
- 若两份 V1.5 文档存在业务语义冲突，以《需求规格说明书与团队开发对接文档》为准。
- 不得依据旧聊天、旧流程图、旧原型或旧实现恢复已废止的业务模型。

## Git 与任务范围

- 所有任务必须从当时最新 `main` 创建独立短期功能分支。
- 禁止直接修改或提交到 `main`。
- 每次只处理当前 Issue/PR 范围，不得进行无关重构、重命名或删除。
- 如果实现必须突破当前 Issue 边界，应立即停止并说明原因，不得静默扩大范围。

## 共享基线

- 公共 API、状态枚举、MongoDB 核心结构、权限规则和公共配置属于共享基线。
- 共享基线变化必须先在 Issue 中冻结，并同步相关负责人。
- 业务状态转换后续必须集中在 Spring Service 层；Controller 和前端不得直接指定最终业务状态。
- 不得提前创建当前 Issue 未要求的业务 Controller、Service、Repository、Document、集合或索引。

## 完成报告

Codex 完成任务后必须报告：

- 新增、修改和删除的文件
- API、配置和数据结构变化
- 实际执行的测试命令与结果
- 当前分支、基线提交和工作区状态
- 是否存在未验证项或范围外影响

禁止批量删除文件或目录。需要删除时只能一次处理一个明确文件路径；需要批量删除时停止并由用户手工处理。
